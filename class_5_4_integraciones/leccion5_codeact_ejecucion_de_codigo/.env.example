# Copy this file to `.env` and fill in your real values. Never commit the real `.env`.
# La única lección de la clase que usa la API de Anthropic (el sandbox de código):
ANTHROPIC_API_KEY=your-anthropic-api-key
# Para el agente de comparación (tool-calling puro):
OPENAI_API_KEY=your-openai-api-key
