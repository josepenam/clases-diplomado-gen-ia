# Copy this file to `.env` and fill in your real values. Never commit the real `.env`.
OPENAI_API_KEY=your-openai-api-key
# Token de LangSmith (el notebook lo lee de LANGSMITH_API_KEY).
LANGSMITH_API_KEY=your-langsmith-api-key
# API key de Packmind: app.packmind.ai > Settings > CLI Authentication (dura 90 días).
PACKMIND_API_KEY_V3=your-packmind-api-key
